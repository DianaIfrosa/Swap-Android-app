<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AppSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/purple_light</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.BachelorThesis.NoActionBar</item>

        <!-- Set status bar and nav bar to avoid having black lines-->
        <item name="android:statusBarColor">@color/purple_light</item>
        <item name="android:navigationBarColor">@color/purple_light</item>
    </style>
</resources>